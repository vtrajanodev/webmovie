import { MovieCard } from "../../components/MovieCard"
import { HomeContainer } from "./Home.styles"

export const Home = () => {
  return (
    <HomeContainer>
      <MovieCard />
      <MovieCard />
      <MovieCard />
      <MovieCard />
      <MovieCard />
      <MovieCard />
    </HomeContainer>
  )
}
