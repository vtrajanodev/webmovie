import { MovieCardContainer, MovieCardInfo } from "./MovieCard.styles"

export const MovieCard = () => {
  return (
    <MovieCardContainer>
      <MovieCardInfo>
        <img src={'img'} alt="" />
      </MovieCardInfo>
    </MovieCardContainer>
  )
}
